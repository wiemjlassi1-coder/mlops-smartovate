# deployment/challenger-deployment.yaml — Challenger deployment (US 4.1)
# Receives 20% of traffic in the A/B testing setup.
# Points to version 2 (the original untuned baseline) for comparison
# against the champion (MLOPS-8 tuned version).

$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json

name: challenger
endpoint_name: breast-cancer-endpoint
description: "Challenger model — original untuned baseline (n_estimators=100, max_depth=5), 20% traffic"

# ── Model: version 2 (original baseline, pre-MLOPS-8 tuning) ──────────────
model: azureml:breast-cancer-classifier:2

# ── Scoring script ─────────────────────────────────────────────────────────
code_configuration:
  code: ../src
  scoring_script: score.py

# ── Environment: same pinned versions (fixes Bug 1) ───────────────────────
environment:
  image: mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu22.04
  conda_file: ../conda.yaml

# ── Compute ────────────────────────────────────────────────────────────────
# ── Compute: E2s_v3 — F1s_v2 failed to schedule (no container logs at all,
# suggesting a placement/availability issue with that SKU in this region).
# Switched to ESv3 family instead, which has substantial free quota (100
# cores) and is officially supported for Managed Online Endpoints.
instance_type: Standard_E2s_v3
instance_count: 1